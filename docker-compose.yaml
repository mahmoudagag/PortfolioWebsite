services:
  portfoliowebsite:
    build:
      context: ./PortfolioWebsite        # Path to the Dockerfile for the backend
      dockerfile: Dockerfile    # Dockerfile name if not 'Dockerfile'
      args:
        # - REACT_APP_BACKEND_URL=http://localhost
        - REACT_APP_BACKEND_URL=http://mahmoudagag.com
        - REACT_APP_PORTFOLIOWEBSITE_PORT=/
        - REACT_APP_WHATSTHEWORDAPP_PORT=/whatstheword
        - REACT_APP_STOCKPAPERTRADINGAPP_PORT=/stockpapertrading
        - REACT_APP_MEALSYAPP_PORT=/mealsy
    ports:
      - "3001:3001"             # Expose backend's port 3000
    restart: always             # Restart policy
    environment:
      - PORT=3001

  whatstheword:
    build:
      context: ./WhatsTheWordApp        # Path to the Dockerfile for the backend
      dockerfile: Dockerfile    # Dockerfile name if not 'Dockerfile'
      args:
        # - REACT_APP_BASE_BACKEND_URL=http://localhost/
        - REACT_APP_BASE_BACKEND_URL=http://mahmoudagag.com/
        - REACT_APP_WEBSOCKET_PATH=/whatstheword/socket.io/

    ports:
      - "5000:5000"             # Expose backend's port 3000
    restart: always             # Restart policy
    environment:
      - PORT=5000

  # stockpapertrading:
  #   build:
  #     context: ./StockPaperTradingApp       # Path to the Dockerfile for the backend
  #     dockerfile: Dockerfile    # Dockerfile name if not 'Dockerfile'
  #     args:
  #       # - REACT_APP_BACKEND_URL=http://localhost/stockpapertrading
  #       - REACT_APP_BACKEND_URL=http://mahmoudagag.com/stockpapertrading
  #       - MONGODB_URI=mongodb+srv://agagmahmoud:xGiowF7dffBIZmPa@cluster0.xrrtozz.mongodb.net/?retryWrites=true&w=majority
  #       - API_KEY=iQmewMFHJY7lz3iha1WrBazQaikCkTZ45iGInxOW
  #       - JWT_ENCRIPTION_KEY=FkdcFb5Dsa
  #   ports:
  #     - "8000:8000"             # Expose backend's port 3000
  #   restart: always             # Restart policy
  #   environment:
  #     - PORT=8000

  mealsy:
    build:
      context: ./Mealsy       
      dockerfile: Dockerfile  
      args:
        - REACT_APP_BACKEND_URL=http://localhost/mealsy
    ports:
      - "7002:7002"            
    restart: always         
    environment:
      - PORT=7002
      - DATABASE_URL=postgresql://mahmoudagag@localhost:5432/mealsy
      - JWT_SECRET=BC5kVNZjOF
      - JWT_LIFETIME=1d
      - API_KEY=j1SvC0/dlV9huEqfruyNRA==iWtoDdyERSYTsHXN

  nginx:
    build:
      context: ./Nginx
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      DOMAIN_NAME: localhost
    depends_on:
      - whatstheword
      - portfoliowebsite
      # - stockpapertrading
      - mealsy
### ASL/ 